
[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]  # line too long — handled by black

[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]
